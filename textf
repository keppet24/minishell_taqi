#include <stdio.h>$
#include <stdlib.h>$
$
int main() {$
    // RM-CM-)cupM-CM-(re la valeur de la variable d'environnement "PATH"$
    int i;$
    i = 0;$
    const char *path = getenv("TOfTO");$
    // VM-CM-)rifie si la variable existe$
    if (path != NULL) {$
        printf("La variable d'environnement PATH vaut :\n%s\n", path);$
    } else {$
        printf("La variable d'environnement PATH n'est pas dM-CM-)finie.\n");$
    }$
    $
    return 0;$
}$
